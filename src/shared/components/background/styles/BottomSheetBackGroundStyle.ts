import {StyleSheet} from 'react-native';

const style = () => {
  return StyleSheet.create({
    container: {
      width: '100%',
      height: '100%',
      backgroundColor: '#000',
      position: 'absolute',
      bottom: 0,
      zIndex: 0,
      opacity: 0.4,
    },
  });
};

export default style;
